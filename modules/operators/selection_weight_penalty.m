function w = selection_weight_penalty(fx, feasible)
% selection_weight_penalty - ????????????????
%
% === ?? 4.2.3 ???????????paper_repro ?????===
% ?????F(x) = 1/f(x)??? f(x) ????F(x) ?????
%
% ?????
%   1. fitness_strict_penalty ?? fx = f(x)????
%   2. ????????? w = 1/f??????? F(x) = 1/f(x)
%   3. ???????? f = f - min(f) + 1e-6 ??????
%   4. ?????????? 0.01*base?????????
%
% ?????????? F(x)=1/f(x) ???????
% ??????????????????????
% ============================================

    w = zeros(size(fx));
    if any(feasible)
        f = fx(feasible);
        f = f - min(f) + 1e-6;  % ????????????????????
        w(feasible) = 1 ./ f;    % ??? F(x) = 1/f(x)
        base = mean(w(feasible));
        w(~feasible) = 0.01 * base;  % ??????????
    else
        f = fx - min(fx) + 1e-6;
        w = 1 ./ f;
    end
    if all(w==0), w(:)=1; end
end

